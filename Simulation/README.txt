Simulation on proteus doesn't have RFID reader as a component
so to simulate it we wrote arduino code the same arduino code but without the sensor library 
we assumed a default ID that arduino sends every second.
Also Note that simulation video dosn't run in real time. 